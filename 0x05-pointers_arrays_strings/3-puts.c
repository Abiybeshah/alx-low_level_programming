#include "main.h"

/**
* _puts - prints a string to stdout
* @str: the input
* Return: void
*/

void _puts(char *str)
{
	char length;
	
	for (length = str ; str != '\0' ; length++)
	{
		_putchar(*str++)'
		_putchar('\n');
	}
}
