#include "main.h"
/**
* infinite_add - adds numbers
* @n1: input for addition
* @n2: input for addition
* @r: input for the buffer
* @size_r: size of the buffer
* Return: r
*/

char *infinite_add(char *n1, char *n2, char *r, int size_r)
{
	int i;
	int j;
	int k[];
	int l[];

	while (n1[i] != '\0')
	{
		k[i] = n1[i];
	}
	return (r);
}
